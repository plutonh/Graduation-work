/**
 * This code is for testing the connection between a website and a reactor
 */
 target Python {
    keepAlive: true
}
import WebSite from "./WebSite.lf"

main reactor {
    w = new WebSite()

    // timer t(1 sec, 1 sec)

    // reaction(t) -> w.number {=
    //     w.number = 1
    // =}
}
